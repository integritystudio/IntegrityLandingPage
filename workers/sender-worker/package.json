{
  "name": "integrity-studio-sender",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "doppler run --project integrity-studio --config dev -- wrangler deploy --env dev",
    "deploy:prd": "doppler run --project integrity-studio --config prd -- wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "doppler run --project integrity-studio --config dev -- vitest run --config vitest.e2e.config.mts",
    "test:live": "doppler run --project integrity-studio --config prd -- vitest run --config vitest.live.config.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.118.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.55.1"
  },
  "dependencies": {
    "jose": "^6.2.7",
    "zod": "^4.4.3"
  },
  "allowScripts": {
    "esbuild": true,
    "fsevents": true,
    "workerd": true
  }
}
