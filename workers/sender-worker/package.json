{
  "name": "integrity-studio-sender",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "doppler run --project integrity-studio --config dev -- wrangler deploy",
    "deploy:prd": "doppler run --project integrity-studio --config prd -- wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "doppler run --project integrity-studio --config dev -- vitest run --config vitest.e2e.config.ts",
    "test:live": "doppler run --project integrity-studio --config dev -- vitest run --config vitest.live.config.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.1",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@vitest/coverage-v8": "^4.1.2",
    "typescript": "^5.3.3",
    "vitest": "^4.1.2",
    "wrangler": "^4.0.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-darwin-arm64": "^4.55.1"
  },
  "dependencies": {
    "jose": "^6.2.2",
    "zod": "^4.3.6"
  }
}
