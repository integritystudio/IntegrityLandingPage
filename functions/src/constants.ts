/** Production site URL (no trailing slash). */
export const BASE_URL = 'https://integritystudio.ai';

/** Default OG image path (relative to BASE_URL). */
export const DEFAULT_OG_IMAGE_PATH = '/images/og-image.png';

/** Default OG image alt text. */
export const DEFAULT_OG_IMAGE_ALT = 'Integrity Studio - AI Observability Platform Dashboard';

/** Site name for og:site_name. */
export const SITE_NAME = 'Integrity Studio';

/** Cache-Control header value for modified HTML responses. */
export const CACHE_CONTROL_NO_CACHE = 'no-cache';

/** X-Robots-Tag header value for noindex routes. */
export const X_ROBOTS_NOINDEX = 'noindex, nofollow';

/** Content-Type header value for HTML. */
export const CONTENT_TYPE_HTML = 'text/html';
